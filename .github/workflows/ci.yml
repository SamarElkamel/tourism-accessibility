name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Install Docker Compose
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/download/v2.24.5/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose
          docker-compose --version
      - name: Build Docker image
        run: docker build -t tourism-app .
      - name: Run ETL and tests
        env:
          MYSQL_DATABASE: tourismdb
          MYSQL_USER: appuser
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          PORT: 8000
        run: |
          docker-compose -f docker/docker-compose.yml up -d db
          docker-compose -f docker/docker-compose.yml run etl
          docker-compose -f docker/docker-compose.yml run app pytest tests/
      - name: Login to Docker Hub
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push to Docker Hub
        if: github.ref == 'refs/heads/main'
        run: |
          docker tag tourism-app ${{ secrets.DOCKER_USERNAME }}/tourism-app:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/tourism-app:latest